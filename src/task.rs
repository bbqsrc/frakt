//! Task types and implementations

// For now, this is a placeholder module
// Tasks will be implemented through the delegate system
// and integrated into the request/response flow

pub(crate) mod data;
pub(crate) mod download;
pub(crate) mod upload;
